import type { Link } from "../lib/prismic-types";

interface CookieNoticeProps {
  acceptButtonLabel?: string;
  cookieTextLabel?: string;
  readMoreButtonLabel?: string;
  readMoreButtonLink?: Link;
}

export default CookieNoticeProps;
